/* #include <stdio.h>
#include <math.h>

int main () {
    int n,fact = 1;

    printf("Enter a number: ");
    scanf("%d", &n);

    for (int i = n;i >= 1;i--){
        fact *= i;        
    }

    printf("The factorial of the number is %d \n", fact );

    return 0;
} */

#include <stdio.h>
#include <math.h>


int main() {
int a=5 , b=10 , c=15 , d=0

int x, y, z, w;

x=(c==a+b||c==d);

y=!(b<=12)\&\alpha a % 2 == 0;

z=(a=0)\&d c==15;

w=c<2;

printf c^{\prime\prime}x=\%d \+ y=\%d \+ z=\%d \t w=\%d^{\prime\prime},\times,y,z,w);
}